export interface TelemetryDTOType {
  time: string;
  lat: string;
  lon: string;
  altitude: string;
  status: string;
  power: string;
}

export interface AltitudeDataType {
  altitude: string;
  time: Date;
}
